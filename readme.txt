This programming uses python3.

To build this we need import package like numpy, pandas and sklearn.

Open the terminal under the folder in the current path and type jupyter notebook to open the solution.ipynb file. Click run to produce the samplesubmission.csv and model.m.